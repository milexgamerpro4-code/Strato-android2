- name: Unzip project ZIP if present
  run: |
    shopt -s nullglob
    ZIP_COUNT=( *.zip )
    if [ ${#ZIP_COUNT[@]} -gt 0 ]; then
      echo "Found zip(s): ${ZIP_COUNT[@]}"
      # prefer strato-master-1.zip if exists
      if [ -f "strato-master-1.zip" ]; then
        unzip -q strato-master-1.zip -d .
      elif [ -f "strato-master.zip" ]; then
        unzip -q strato-master.zip -d .
      else
        unzip -q "${ZIP_COUNT[0]}" -d .
      fi
    else
      echo "No zip found, assuming files already in repo"
    fi
    ls -la
